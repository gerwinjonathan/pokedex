import 'package:flutter/widgets.dart';

class MorePokeScreens extends StatelessWidget {
  const MorePokeScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: ListView(
        children: [],
      ),
    );
  }
}
